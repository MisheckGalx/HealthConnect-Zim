document.addEventListener('DOMContentLoaded', function() => {
  // Eaxample of adding dynamic content to the page
  const appDiv = document.getElementById('app');
  appDiv.innerHTML = '<h2>Makadini/Unjani welcome to HealthConnect Zim!</h2><p>Your gateway to quality healthcare services.</p>';
})
